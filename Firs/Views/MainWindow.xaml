<Window x:Class="First.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:First"
        xmlns:sys="clr-namespace:System;assembly=mscorlib"
        xmlns:local1="clr-namespace:First"
        mc:Ignorable="d"
        Title="Window1" Height="1000" Width="1000">
    <Grid Margin="0,0,-27,-43">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="286*"/>
            <ColumnDefinition Width="733*"/>
        </Grid.ColumnDefinitions>
        <ListView x:Name="list" FontSize="18" Margin="-6,10,523,332" Grid.ColumnSpan="2">
            <Button Background="LawnGreen"  Content="Добавить задачу" Width="486" Click="Button_Click_1" />
            <Button  Background="GreenYellow" Content="Просмотра списка задач" Width="485" Click="Button_Click_2"/>
            <Button Background="LightGreen" Content="Возможнгсть редакции выбранной задачи" Width="484"/>
            <TextBox x:Name="one" Text="{Binding ElementName=picker, Path=X_Title}"></TextBox> T
        </ListView>
       
    </Grid>
</Window>
